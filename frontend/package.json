{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run build-css",
    "build-css": "cross-env CHOKIDAR_USEPOLLING=1 tailwindcss -i ./src/input.css -o ./public/style.css --watch"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "tailwindcss": "^3.4.1"
  }
}
